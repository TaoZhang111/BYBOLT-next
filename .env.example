# Public site URL used for canonical links and social metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Public URL of the separately deployed no-database admin Worker.
# The deployed project URL below is also the application's built-in default.
NEXT_PUBLIC_ADMIN_API_URL=https://bybolt-admin-api.tao1461248574.workers.dev

# RFQ notification and anti-spam values will be configured later.
SALES_EMAIL=
EMAIL_API_KEY=
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
